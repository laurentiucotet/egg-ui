import { DesignTokenDocBlock } from 'storybook-design-token';

# Design tokens

This story demonstrates the design tokens available in the project. The CSS file `src/design-tokens.css` contains annotated variables that the addon picks up automatically.

<DesignTokenDocBlock categoryName="Colors" viewType="card" />

<DesignTokenDocBlock categoryName="Spacing" viewType="table" />

## Button — relevant tokens

The block below shows only the color tokens used by the `Button` component.

<DesignTokenDocBlock
	categoryName="Colors"
	viewType="card"
	filterNames={[
		'--color-primary',
		'--color-primary-hover',
		'--color-primary-active',
		'--color-text-inverse',
	]}
	/>
