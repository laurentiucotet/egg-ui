import { tv } from 'tailwind-variants'

export const registerFormContainer = tv({
  base: [
    // Base container styles
    'w-full',
  ],
})

export const registerFormContent = tv({
  base: [
    // Form content spacing
    'space-y-4',
  ],
})

export const registerFormInputGroup = tv({
  base: [
    // Input group spacing
    'space-y-4',
  ],
})

export const registerFormButton = tv({
  base: [
    // Button container styles
    'w-full',
  ],
})
