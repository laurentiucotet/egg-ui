import { tv } from 'tailwind-variants'

export const loginFormContainer = tv({
  base: [
    // Base container styles
    'w-full',
  ],
})

export const loginFormContent = tv({
  base: [
    // Form content spacing
    'space-y-4',
  ],
})

export const loginFormInputGroup = tv({
  base: [
    // Input group spacing
    'space-y-4',
  ],
})

export const loginFormButton = tv({
  base: [
    // Button container styles
    'w-full',
  ],
})
